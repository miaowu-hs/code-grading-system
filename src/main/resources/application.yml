server:
  port: 8081

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/code_grading_db
    username: root
    password: 789***tyu(*
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    com.example.codegradingsystem: DEBUG