spring:
  datasource:
    url: jdbc:mysql://localhost:3306/code_grading_system?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 789***tyu(*就是一个符号
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect

logging:
  level:
    com.example.codegradingsystem: INFO
    org.springframework.web: INFO