version: '3.8'
services:
  code-grading-backend:
    build: .
    ports:
      - "8085:8085"
    environment:
      - spring.ai.dashscope.api-key=sk-sp-dc98f3eb5f524bbca679af6956d1bb0c
    networks:
      - code-grading-network
    restart: unless-stopped

networks:
  code-grading-network:
    driver: bridge